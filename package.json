{
  "name": "job-alert-2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:backend": "pnpm --filter @job-alert/backend dev",
    "dev:frontend": "pnpm --filter @job-alert/frontend dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "clean": "pnpm -r exec rimraf dist"
  },
  "devDependencies": {
    "prisma": "^7.5.0",
    "tsx": "^4.21.0",
    "typescript": "~5.9.3"
  }
}
